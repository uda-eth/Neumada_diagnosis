Your test file is structured correctly, but there are a few **potential issues** that could be causing Jest to **not detect or execute** the tests properly. Let's go through a **debugging process** step by step.

---

## **🔍 Issues & Fixes**
### **1️⃣ `app` Might Not Be Defined in First `describe` Block**
#### **Problem:**  
Your first `describe('Auth API', ...)` block is using `app`, but you **haven't initialized it yet**.

#### **Fix:**  
Move the **`app` definition outside of `describe`** and initialize it **before all tests**:  

```ts
import request from "supertest";
import { Express } from "express";
import { createApp } from "../../server/app";

let app: Express;

beforeAll(async () => {
  const { app: expressApp } = await createApp();
  app = expressApp;
});
```
✅ This ensures **all test cases** use the same `app` instance.

---

### **2️⃣ Tests Might Be Running Before Server Starts**
#### **Problem:**  
If `createApp()` initializes an **async Express app**, Jest **might start tests before the server is ready**.

#### **Fix:**  
Force Jest to **wait for the app to be initialized** before running tests:  

```ts
beforeAll(async () => {
  const { app: expressApp } = await createApp();
  app = expressApp;
}, 10000); // ⬅ Extends timeout to 10 seconds
```
✅ This prevents "server not available" issues.

---

### **3️⃣ Jest May Not Detect Tests in `tests/` Folder**
#### **Problem:**  
By default, Jest looks for test files in `__tests__/`, `*.test.ts`, or `*.spec.ts` inside `src/`.  
If your tests are in `tests/server/`, Jest might **not be configured** to look there.

#### **Fix:**  
Modify **`jest.config.ts`** to explicitly include the `tests/` directory:  

```ts
export default {
  preset: "ts-jest",
  testEnvironment: "node",
  testMatch: ["<rootDir>/tests/**/*.test.ts"],
  collectCoverage: true,
  coverageDirectory: "coverage",
};
```
✅ This tells Jest to run **all tests inside `/tests`**.

---

### **4️⃣ Check If Jest Is Skipping Tests**
#### **Problem:**  
Jest **skips** tests if:
- `test.only(...)` is used (which excludes other tests).
- The file doesn't contain **at least one valid test case**.

#### **Fix:**  
1. **Ensure no `.only()` is present**:
```ts
describe.only("Auth API", () => { ... }); // ❌ This excludes all other tests
```
Replace `.only` with just `describe`:
```ts
describe("Auth API", () => { ... }); // ✅ Fix
```
2. **Run Jest in verbose mode to debug skipped tests**:
```sh
npm test -- --verbose
```
✅ This will **show which test files Jest detects**.

---

### **5️⃣ Check If Jest Is Stuck on Open Handles**
#### **Problem:**  
If Jest detects **unclosed async operations**, it **doesn’t execute tests**.

#### **Fix:**  
Run:
```sh
npx jest --detectOpenHandles
```
✅ This helps find **stuck database connections or open API calls**.

---

### **6️⃣ Ensure Jest & TypeScript Are Installed Correctly**
#### **Problem:**  
If Jest isn't properly installed, it **won't detect tests**.

#### **Fix:**  
Run:
```sh
npm list --depth=0 | grep jest
```
It should return something like:
```
jest@xx.x.x
ts-jest@xx.x.x
@types/jest@xx.x.x
```
If missing, reinstall Jest:
```sh
npm install --save-dev jest ts-jest @types/jest supertest
```
✅ This ensures Jest is **properly installed**.

---

## **🚀 Next Steps**
1️⃣ **Fix `app` Initialization**
   - Move `app` initialization to `beforeAll()` at the top.  
   - Increase Jest timeout to **ensure server starts before tests run**.  

2️⃣ **Ensure Jest Can Detect Tests**
   - Update `jest.config.ts` to include `"testMatch": ["<rootDir>/tests/**/*.test.ts"]`.  

3️⃣ **Check Jest Debugging Info**
   - Run Jest with `--verbose` to **see if tests are detected**:
     ```sh
     npm test -- --verbose
     ```
   - Run Jest with `--detectOpenHandles` to **find async issues**:
     ```sh
     npx jest --detectOpenHandles
     ```
     
4️⃣ **Ensure Jest is Installed**
   - Run `npm list --depth=0 | grep jest` to verify Jest packages.

---

🚀 **Try these fixes and let me know if Jest starts running your tests!** If issues persist, show me the Jest error output and I'll help debug further.